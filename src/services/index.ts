/**
 * Service layer entry point.
 *
 * Services contain all business logic and orchestrate calls to
 * repositories / the in-memory store. They must not import Express types.
 *
 * Services are co-located with their resource module:
 *   src/api/v1/[resource]/[resource].service.ts
 *
 * Implemented services:
 *   - src/api/v1/users/user.service.ts
 *
 * Planned services (to be added in upcoming sprints):
 *   - tasks.service.ts
 *   - dependency.service.ts
 *   - assignment.service.ts
 *   - auth.service.ts
 *   - reports.service.ts
 */
export {};
