/**
 * Domain model entry point.
 *
 * Models define the shape of persisted entities.
 * They are co-located with their resource module:
 *   src/api/v1/[resource]/[resource].model.ts
 *
 * Implemented models:
 *   - src/api/v1/users/user.model.ts  (UserModel)
 *
 * Planned models (to be added in upcoming sprints):
 *   - task.model.ts        (TaskModel)
 *   - dependency.model.ts  (TaskDependencyModel)
 *   - assignment.model.ts  (AssignmentHistoryModel)
 *   - audit.model.ts       (AuditLogModel)
 */
export {};
