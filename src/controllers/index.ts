/**
 * Controller layer entry point.
 *
 * Controllers handle HTTP request/response concerns only —
 * no business logic. They delegate work to services.
 *
 * Controllers are co-located with their resource module:
 *   src/api/v1/[resource]/[resource].controller.ts
 *
 * Implemented controllers:
 *   - src/api/v1/health/health.controller.ts  (GET /api/v1/health)
 *
 * Planned controllers (to be added in upcoming sprints):
 *   - tasks.controller.ts
 *   - users.controller.ts
 *   - auth.controller.ts
 *   - reports.controller.ts
 */
export {};
